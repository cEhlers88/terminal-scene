export interface IPadding {
    bottom:number,
    left:number,
    right:number,
    top:number
}
export interface IRect {
    x:number,
    y:number,
    w:number,
    h:number
}
export interface IRgbValue {
    b:number,
    g:number,
    r:number
}